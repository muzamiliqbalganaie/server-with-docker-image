services:
  app:
    build: .
    env_file: .env
    ports:
      - "${PORT}:${PORT}"
    restart: unless-stopped
    volumes:
      - ./:/app:delegated
    command: npm start
  
  # Bless Network node - uncomment when image is available
  # blessnode:
  #   image: ghcr.io/blessnetwork/b7s:0.6.6.patch3
  #   container_name: blessnetwork-node
  #   env_file:
  #     - .env
  #   ports:
  #     - "${BLESS_PORT}:9527"
  #   restart: unless-stopped
  #   volumes:
  #     - ./bless_data:/data
